# Task1.py
  In this the file is tried to open if there is no error it will open or else the exception is handled.<br />
  file contents are read and printed to console
#  Task2.py
  1.File is written which is accessed using the **'W'** permission to ensure to remove any previous content<br />
  
  2.File is then accessed using the **'r'** permission to read the contents and written into the console.<br />

  3.File is then accessed using the **'r'** permission  to append into the file bu taking user input<br >
  
  4.File is then accessed using the **'r'** permission to read the contents and written into the console.<br />

**file1.txt**
stores the data and file that is being used to perform operations.
  
